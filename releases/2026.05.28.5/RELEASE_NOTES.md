# 2026.05.28.5

- Framework-dependent Windows x64 packages using shared DLLs.
- Requires the .NET 8 runtime.
- CLIs support `--version`.
- Includes the standalone offline corpus probe package.

Release metadata:

- `manifest.json`
- `checksums.sha256`
